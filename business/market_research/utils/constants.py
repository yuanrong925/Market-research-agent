"""
【市场调研专属】全局常量定义

集中管理所有跨模块共享的约束规则文本，确保各节点 prompt 一致。
"""

# ============================================================
#  仅 PDF 模式硬性约束规则
#  （所有节点复用同一套文本，防止文案参差不齐）
# ============================================================

PDF_ONLY_RULE = """
【硬性强制规则：仅PDF模式】
1. 不允许引入任何文档以外的互联网信息；
2. 禁止生成、标注「网络资料」类来源；
3. 所有引用素材只能归属为【文档资料】；
4. 不得补充PDF原文不存在的数据、观点、案例。
"""

# ============================================================
#  Writer 节点补充约束（仅 PDF 模式）
# ============================================================

PDF_ONLY_WRITER_CONSTRAINT = """
【仅 PDF 模式约束】
当前模式为仅 PDF 模式，所有分析内容只能基于上传的 PDF 文档。
1. 所有引用素材只能标记为【文档资料】，禁止生成"网络资料"来源条目
2. 报告中不得出现任何网络来源的引用（如 S1/S2/S3 等标注为网络资料的条目）
3. 信息来源附录中只能包含 PDF 文档来源，禁止出现网络链接
4. 禁止引入任何文档以外的互联网信息
5. 禁止自行扩充文档不存在的行业数据、预测信息，仅对原文信息整理、总结
"""

# ============================================================
#  网页清洗相关常量
# ============================================================
# 最大网页清洗数量（联网搜索后，最多清洗前 N 个页面）
MAX_WEB_PAGES_TO_CLEAN = 5

# ============================================================
#  相关性阈值配置（检索后二分判定用）
# ============================================================
# 原则：只在文档和问题完全不相关时切断；素材不足/时间缺失不终止
# 分数线统一放到配置常量，不零散写在代码各处
#
# RETRIEVAL_IRRELEVANT_THRESHOLD: 全部结果低于此阈值 → 完全无关
# RETRIEVAL_INSUFFICIENT_THRESHOLD: 全部结果低于此阈值但高于无关阈值 → 素材不足
# RETRIEVAL_RELEVANT_THRESHOLD: 存在结果高于此值 → 正常继续
RETRIEVAL_IRRELEVANT_THRESHOLD = 0.5
RETRIEVAL_INSUFFICIENT_THRESHOLD = 0.7
RETRIEVAL_RELEVANT_THRESHOLD = 0.7

# 本地模型（Ollama）相关性阈值 — 更宽松，因为本地 embedding 质量低于云端
# 本地模型向量相似度通常在 0.4~0.6 范围，放宽阈值避免误阻断
LOCAL_RETRIEVAL_IRRELEVANT_THRESHOLD = 0.35
LOCAL_RETRIEVAL_INSUFFICIENT_THRESHOLD = 0.5
LOCAL_RETRIEVAL_RELEVANT_THRESHOLD = 0.5


# ============================================================
#  FactChecker 仅 PDF 模式约束（核查专用）
# ============================================================

PDF_ONLY_CHECKER_RULE = """
【仅 PDF 模式约束】
当前模式为仅 PDF 模式，所有分析内容只能基于上传的 PDF 文档。
4. 禁止引用任何文档以外的互联网信息，报告中不得出现任何网络来源的引用条目
5. 所有引用素材只能标记为【文档资料】，禁止生成"网络资料"来源条目
6. 事实核查只检查 PDF 文档中的原文支撑，不得引用外部资料进行核查
7. 不得补充PDF原文不存在的数据、观点、案例
"""

# ============================================================
#  Rewrite 仅 PDF 模式约束（重写专用）
# ============================================================

PDF_ONLY_REWRITE_RULE = """
【仅 PDF 模式约束】
当前模式为仅 PDF 模式，所有分析内容只能基于上传的 PDF 文档。
- 所有引用素材只能标记为【文档资料】，禁止生成"网络资料"来源条目
- 报告中不得出现任何网络来源的引用（如 S1/S2/S3 等标注为网络资料的条目）
- 事实核查不得引用外部资料，只能基于 PDF 文档中的原文
- 不得补充PDF原文不存在的数据、观点、案例
"""


# ============================================================
#  后置段落校验相关常量
# ============================================================

# 后置校验：单段 LLM 超时（秒）
POST_CHECK_PARAGRAPH_TIMEOUT = 8

# 后置校验：全局时间硬上限（秒），超过此值跳过整个后置校验
POST_CHECK_GLOBAL_TIMEOUT = 90

# 后置校验：最短有效段落长度（字符数，strip 后）
POST_CHECK_MIN_PARAGRAPH_LENGTH = 80

# 后置校验：最大重写次数
POST_CHECK_MAX_REWRITE = 2

# 后置校验：向量召回 top_k
POST_CHECK_RECALL_TOP_K = 5

# 后置校验：向量召回最低有效 chunk 数（低于此值跳过校验）
POST_CHECK_MIN_CHUNKS = 5